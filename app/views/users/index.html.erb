<div class="container">
    <% @users.each do |user| %>
        <% if user.days.ids.include?(@day)  %>

            <%= link_to user.username, user_path(user) %>

        <% end %>
    <% end %>
</div>